<Window x:Class="YAAM.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YAAM.App.ViewModels"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="Yet Another Autostart Manager" Height="600" Width="900"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources> 
        <!-- This converter is needed to hide/show the loading overlay -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Window.Resources>

    <Grid>
        <!-- Main Layout Grid -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Toolbar -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
                <Button Content="Load Items" 
                        Command="{Binding LoadItemsCommand}" 
                        IsEnabled="{Binding IsNotBusy}" 
                        Padding="10,5"/>
                <!-- Add and Modify buttons would require a dialog service to work properly -->
                <Button Content="Add New..." 
                        IsEnabled="{Binding IsNotBusy}"
                        Command="{Binding CreateItemCommand}"
                        Padding="10,5" 
                        Margin="5,0,0,0"
                        ToolTip="Create a new autostart item"/>
            </StackPanel>

            <!-- DataGrid to display autostart items -->
            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">
                
                <DataGrid.Columns>
                    <!-- Column for enabling/disabling items -->
                    <DataGridTemplateColumn Header="Enabled" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsEnabled, Mode=OneWay}"
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:InvokeCommandAction Command="{Binding DataContext.ToggleEnableStateCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                   CommandParameter="{Binding }"/>
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </CheckBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Other data columns -->
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*"/>
                    <DataGridTextColumn Header="Executable Path" Binding="{Binding ExecutablePath}" Width="3*"/>
                    <DataGridTextColumn Header="Arguments" Binding="{Binding Arguments}" Width="1*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="1*"/>
                    <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="3*"/>

                    <!-- Column for actions like Delete -->
                    <DataGridTemplateColumn Header="Actions" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>

        </Grid>
        
        <!-- Loading Overlay -->
        <Grid Background="#80000000"
              Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Loading..." 
                           Foreground="White" 
                           FontSize="24"
                           Margin="0,0,0,10"/>
                <ProgressBar IsIndeterminate="True" Width="200"/>
            </StackPanel>
        </Grid>

    </Grid>
</Window>