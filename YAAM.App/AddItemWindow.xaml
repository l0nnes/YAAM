<Window x:Class="YAAM.App.AddItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YAAM.App.ViewModels"
        mc:Ignorable="d"
        Title="Add New Autostart Item"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        d:DataContext="{d:DesignInstance Type=local:AddItemViewModel, IsDesignTimeCreatable=True}"
        MinWidth="450"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" />
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Executable Path:" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ExecutablePath, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Command="{Binding BrowseCommand}" Padding="5,2" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Arguments:" />
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2"
                 Text="{Binding Arguments, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Type:" />
        <ComboBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2"
                  ItemsSource="{Binding AutostartTypes}"
                  SelectedItem="{Binding SelectedType}" />

        <!-- This ComboBox is only visible when Registry is selected -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Scope:">
            <TextBlock.Style>
                <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedType}" Value="Registry">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
        <ComboBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2"
                  ItemsSource="{Binding RegistryScopes}"
                  SelectedItem="{Binding SelectedRegistryScope}">
            <ComboBox.Style>
                <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedType}" Value="Registry">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ComboBox.Style>
        </ComboBox>

        <CheckBox Grid.Row="5" Grid.Column="1" Content="Enabled on creation" IsChecked="{Binding IsEnabled}" Margin="5"
                  VerticalAlignment="Center" />

        <!-- OK and Cancel Buttons -->
        <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Command="{Binding SaveCommand}" IsDefault="True" MinWidth="80" Margin="0,0,10,0" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" IsCancel="True" MinWidth="80" />
        </StackPanel>
    </Grid>
</Window>